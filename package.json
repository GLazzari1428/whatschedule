{
  "name": "whatscheduler",
  "version": "2.0.0",
  "description": "Self-hosted WhatsApp message scheduler",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "cap:init": "npx cap init",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:build": "npx cap sync && cd android && ./gradlew assembleRelease",
    "cap:build:debug": "npx cap sync && cd android && ./gradlew assembleDebug"
  },
  "keywords": [
    "whatsapp",
    "scheduler",
    "automation",
    "self-hosted",
    "messaging"
  ],
  "author": "Gustavo Lazzari",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/GLazzari1428/whatscheduler"
  },
  "dependencies": {
    "better-sqlite3": "^9.2.2",
    "body-parser": "^1.20.2",
    "express": "^4.18.2",
    "node-cron": "^3.0.3",
    "qrcode": "^1.5.3",
    "whatsapp-web.js": "^1.34.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/cli": "^7.4.4",
    "@capacitor/core": "^7.4.4",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
